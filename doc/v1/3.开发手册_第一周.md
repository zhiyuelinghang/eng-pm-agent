# 开发手册 · 第一周垂直切片（新手版，照着敲）

> 读者：3 位刚接触 AgentScope / Hermes / RAGFlow 的开发者
> 目标：一周内做出一条端到端跑通的切片，验收线 = **项目总工拿不到财务、项目经理拿得到（直连 + Hermes 委派两条路都成立）**
> 用法：每个人先读 **Part 0 + Part 1 + Part 2**，然后只精读自己那一 Part（P1/P2/P3），Day 4 再一起看 Part 6。

---

## ⚠️ 给新手的三句实话

1. **一周是紧的**。如果某天卡住，优先保证"主链路能跑"，把好看的东西（样式、额外功能）全部砍掉。卡 2 小时还没解决就贴到群里求助，别一个人耗。
2. **先能跑，再优雅**。本手册里的代码是"能跑的最简版"，不是最佳实践。先让它亮起来，第二周再重构。
3. **每一步都有"✅ 验证"**。没看到预期输出就不要往下走——错误越早发现越好修。

---

## Part 0 · 整体约定（全员必读）

### 0.1 一个共享的大模型 API Key（省事关键）
三个工具都要调大模型。**全队统一用一个 provider**，避免各配各的。推荐二选一：
- **阿里云百炼 DashScope**（AgentScope 原生支持，国内访问稳）：注册拿 `DASHSCOPE_API_KEY`，模型用 `qwen-plus`
- 或 **DeepSeek**（OpenAI 兼容，便宜）：拿 `DEEPSEEK_API_KEY`

本手册代码以 **DashScope** 为例。把 key 发到群里共享（内部 demo 阶段够用，正式上线再各自管理）。

> 注意：Hermes 要求模型上下文 ≥ 64K，Qwen-plus / DeepSeek 都满足。

### 0.2 服务端口分配（记牢，集成时全靠它）
| 服务 | Owner | 端口 |
|---|---|---|
| 前端 | P3 | 3000 |
| Gateway + BFF | P2 | 8000 |
| 编排 Orchestrator | P2 | 8010 |
| 检索服务 | P1 | 8001 |
| Hermes 委派服务 | P3 | 8002 |
| Hermes API Server | P3 | 8088 |
| RAGFlow | P1 | 9380（Web UI: 80 或自定义）|

### 0.3 两个测试账号（写死在 roles.yaml，Day 1 定）
```
pm_zhang  / 123456  → 项目经理 PROJECT_MANAGER（看得到财务）
ce_li     / 123456  → 项目总工 CHIEF_ENGINEER（看不到财务）
```

---

## Part 1 · 环境准备（Day 0，全员各自机器上做一遍）

> 操作系统：建议 **macOS 或 Linux**（或 Windows 装 WSL2）。Hermes 在原生 Windows 支持不稳，P3 务必用 WSL2 或 Linux/Mac。

### 1.1 装基础工具
```bash
# 1) Python 3.10+（检查版本）
python3 --version        # 应 >= 3.10，否则去 python.org 装

# 2) Git
git --version

# 3) Docker + Docker Compose（RAGFlow 和各服务要用）
docker --version
docker compose version   # 注意是 "docker compose" 不是 "docker-compose"

# 4) Node.js 18+（前端 + Hermes 部分工具要用）
node --version
```
缺哪个装哪个：Docker 去 docker.com 下 Desktop；Node 去 nodejs.org。

**✅ 验证**：上面四条命令都能打印出版本号。

### 1.2 建一个干净的 Python 虚拟环境（每个 Python 服务都用它）
```bash
python3 -m venv venv
source venv/bin/activate          # Windows WSL 同此；退出用 deactivate
pip install --upgrade pip
```
**✅ 验证**：命令行前面出现 `(venv)`。

### 1.3 装本周要用的 Python 包
```bash
pip install fastapi "uvicorn[standard]" pyjwt pyyaml requests python-multipart
```
框架包各自 Part 里再装（避免一次装太多报错难定位）。

---

## Part 2 · 仓库与协作搭建（Day 1，P2 主导，全员围观学 Git）

### 2.1 建 GitHub 仓库
P2 在 GitHub 网页点 **New repository** → 名字 `eng-pm-agent` → Private → 勾 "Add a README" → Create。

三人都把仓库克隆到本地：
```bash
git clone https://github.com/<你们组织>/eng-pm-agent.git
cd eng-pm-agent
```

### 2.2 建目录结构
P2 在本地建好下面的空目录和占位文件，然后推上去：
```bash
mkdir -p contracts common gateway orchestrator kb hermes ingestion/wechat frontend docker .github/workflows
touch contracts/.gitkeep common/.gitkeep gateway/.gitkeep orchestrator/.gitkeep \
      kb/.gitkeep hermes/.gitkeep ingestion/wechat/.gitkeep frontend/.gitkeep docker/.gitkeep
```
目录归属：`kb/`→P1，`gateway/ orchestrator/ common/`→P2，`hermes/ ingestion/ frontend/`→P3，`contracts/`→共享。

### 2.3 写共享契约（本周成败关键，今天必须定死）

**① `contracts/roles.yaml`** — 角色、账号、权限，全系统的事实来源：
```yaml
# contracts/roles.yaml
users:
  pm_zhang: { password: "123456", role: "PROJECT_MANAGER", name: "张经理" }
  ce_li:    { password: "123456", role: "CHIEF_ENGINEER", name: "李总工" }

roles:
  PROJECT_MANAGER:
    level: 100
    datasets: ["ds_public_docs", "ds_specs", "ds_wechat", "ds_finance"]  # 全部
    skills:   ["brief", "spec_query"]
    deny:     []
  CHIEF_ENGINEER:
    level: 50
    datasets: ["ds_public_docs", "ds_specs", "ds_wechat"]                # 无 ds_finance
    skills:   ["brief", "spec_query"]
    deny:     ["kb:finance", "mcp:finance"]
```

**② `common/auth.py`** — 令牌签发/校验，三个 Python 服务都 import 它：
```python
# common/auth.py  —— 共享：签发和校验能力令牌（JWT）
import jwt, time, yaml, os

SECRET = "dev-secret-change-me"      # 本周用固定值，上线再换
ALGO = "HS256"

def _load_roles():
    here = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
    with open(os.path.join(here, "contracts", "roles.yaml"), encoding="utf-8") as f:
        return yaml.safe_load(f)

def issue_token(user_id: str) -> dict:
    cfg = _load_roles()
    user = cfg["users"][user_id]
    role = cfg["roles"][user["role"]]
    payload = {
        "sub": user_id,
        "role": user["role"],
        "datasets": role["datasets"],
        "skills": role["skills"],
        "deny": role["deny"],
        "exp": int(time.time()) + 3600,   # 1 小时有效
    }
    return {"token": jwt.encode(payload, SECRET, algorithm=ALGO), "name": user["name"], "role": user["role"]}

def verify_token(token: str) -> dict:
    """校验并返回 payload；失败抛异常。"""
    return jwt.decode(token, SECRET, algorithms=[ALGO])
```

**③ `contracts/API.md`** — 把三个 HTTP 接口写清楚（贴第一周开发计划里的契约 B/C，再加登录）：
```
POST :8000/auth/login      Body {user, password}         → {token, name, role}
POST :8010/chat            Header Bearer  Body {message}  → {reply, sources, via}
POST :8001/kb/retrieve     Header Bearer  Body {query, top_k} → {chunks:[{text,source,dataset,score}]}
POST :8002/delegate        Header Bearer  Body {task, skill} → {result, skill_used}
```
**改这三个文件需要两人 review**（在 PR 里 @ 对方）。

### 2.4 Git 协作最简流程（新手记住这 6 条）
```bash
git checkout main && git pull            # 1. 开工前先拉最新
git checkout -b feat/kb-retrieve         # 2. 开自己的分支（feat/模块-功能）
# ...写代码...
git add . && git commit -m "[kb] 检索服务初版"   # 3. 提交（commit 前缀标模块）
git push -u origin feat/kb-retrieve      # 4. 推上去
# 5. 去 GitHub 网页点 "Compare & pull request"，@ 一个人 review
# 6. 对方 Approve 后点 Merge
```
规矩：`main` 不准直接推；每天至少合并一次，别攒到周四。

**✅ Day 1 验证**：三人都能克隆、都能推一个测试分支并合并成功；`contracts/` 三个文件在 main 上。

---

## Part 3 · P1 手册 · 知识层 + 权限检索服务

你的产出：RAGFlow 跑起来、建好 4 个数据集、灌入样本、做一个**按角色过滤**的检索服务。

### 3.1 启动 RAGFlow（Docker）
```bash
# 在任意目录
git clone https://github.com/infiniflow/ragflow.git
cd ragflow/docker
docker compose -f docker-compose.yml up -d      # 第一次拉镜像较慢，耐心等
```
等 2-3 分钟后浏览器打开 `http://localhost`（或文档里写的端口），注册一个管理员账号登录。

**✅ 验证**：能打开 RAGFlow Web 界面并登录。
> 卡住常见原因：内存不足（RAGFlow 建议 ≥16G）、端口被占。看 Part 8。

### 3.2 建 4 个数据集（用 Web UI，最直观）
在 RAGFlow 界面 **Knowledge Base → Create**，建 4 个，名字**必须和 roles.yaml 完全一致**：
```
ds_public_docs   （项目资料）
ds_specs         （施工规范）
ds_wechat        （微信记录，等 P3 给数据）
ds_finance       （财务，敏感）★单独一个，这是隔离的硬边界
```
每个建好后上传几份样本文档（PDF/Word/txt 都行），点 **Parse** 等它解析完成（状态变绿）。

**财务集放点能验证的内容**：比如建个 txt 写"本项目总成本 1.2 亿元，已付款 8000 万元"，传进 `ds_finance`。

**✅ 验证**：4 个数据集都有文档且解析成功（done 状态）。

### 3.3 拿 RAGFlow API Key
RAGFlow 界面右上头像 → **API** → 复制 API Key，记为 `RAGFLOW_API_KEY`。

### 3.4 写检索服务 `kb/retrieve_service.py`
核心逻辑：解析令牌拿到该用户允许的数据集列表 → 只在这些数据集里检索。**总工的令牌里压根没有 ds_finance，所以查不到财务**。
```python
# kb/retrieve_service.py
import os, sys, requests
from fastapi import FastAPI, Header, HTTPException
from pydantic import BaseModel

sys.path.append(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from common.auth import verify_token

RAGFLOW_BASE = os.environ.get("RAGFLOW_BASE", "http://localhost:9380")
RAGFLOW_API_KEY = os.environ["RAGFLOW_API_KEY"]   # 启动前 export

app = FastAPI()

# 数据集名 → RAGFlow 内部 ID 的映射（Day 1 手动填一次）
# 在 RAGFlow 每个数据集页面 URL 里能看到 id，或用 list 接口拿
DATASET_IDS = {
    "ds_public_docs": "填入真实ID",
    "ds_specs":       "填入真实ID",
    "ds_wechat":      "填入真实ID",
    "ds_finance":     "填入真实ID",
}

class Req(BaseModel):
    query: str
    top_k: int = 6

@app.post("/kb/retrieve")
def retrieve(req: Req, authorization: str = Header(...)):
    try:
        payload = verify_token(authorization.replace("Bearer ", ""))
    except Exception:
        raise HTTPException(401, "令牌无效")

    # 关键：只取该角色允许的数据集
    allowed_names = payload["datasets"]
    allowed_ids = [DATASET_IDS[n] for n in allowed_names if DATASET_IDS.get(n)]

    # 调 RAGFlow 检索 HTTP API
    r = requests.post(
        f"{RAGFLOW_BASE}/api/v1/retrieval",
        headers={"Authorization": f"Bearer {RAGFLOW_API_KEY}"},
        json={"question": req.query, "dataset_ids": allowed_ids,
              "top_k": req.top_k, "similarity_threshold": 0.2},
        timeout=30,
    )
    data = r.json().get("data", {})
    chunks = [
        {"text": c.get("content", ""), "source": c.get("document_keyword", ""),
         "dataset": c.get("dataset_id", ""), "score": c.get("similarity", 0)}
        for c in data.get("chunks", [])
    ]
    return {"chunks": chunks}
```
启动：
```bash
export RAGFLOW_API_KEY="你的key"
uvicorn kb.retrieve_service:app --port 8001 --reload
```

**✅ 验证**（需要 P2 的 gateway 先能发令牌；或先用下面的临时令牌脚本自测）：
```bash
# 临时自测：用 common.auth 直接造两个令牌
python3 -c "import sys; sys.path.append('.'); from common.auth import issue_token; print('PM:', issue_token('pm_zhang')['token']); print('CE:', issue_token('ce_li')['token'])"

# 用 CE(总工)令牌查财务，应查不到；用 PM 令牌查，应查到
curl -X POST localhost:8001/kb/retrieve -H "Authorization: Bearer <CE令牌>" \
     -H "Content-Type: application/json" -d '{"query":"项目总成本"}'
```
总工返回的 chunks 里**不应**出现财务内容。这就是你这周最重要的产出。

### 3.5 入库微信数据（等 P3 的 JSONL）
P3 给你 `wechat.jsonl` 后，把里面的 text 拼成 txt 或直接用 RAGFlow 上传 JSONL，灌进 `ds_wechat`。简单做法：写个小脚本把每条消息转成一行文本再传。本周量小，UI 手动传也行。

---

## Part 4 · P2 手册 · 权限闸 + 编排（最重的一摊，加油）

你的产出：登录发令牌的 Gateway + 把检索/委派串起来的 Orchestrator。

### 4.1 Gateway `gateway/app.py`（登录 + 发令牌 + 兼当前端入口 BFF）
```python
# gateway/app.py
import os, sys, requests
from fastapi import FastAPI, Header, HTTPException
from fastapi.middleware.cors import CORSMiddleware
from fastapi.staticfiles import StaticFiles
from pydantic import BaseModel

sys.path.append(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from common.auth import issue_token

ORCH = os.environ.get("ORCH_URL", "http://localhost:8010")
app = FastAPI()
app.add_middleware(CORSMiddleware, allow_origins=["*"], allow_methods=["*"], allow_headers=["*"])

class Login(BaseModel):
    user: str
    password: str

@app.post("/auth/login")
def login(body: Login):
    import yaml
    here = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
    cfg = yaml.safe_load(open(os.path.join(here, "contracts", "roles.yaml"), encoding="utf-8"))
    u = cfg["users"].get(body.user)
    if not u or u["password"] != body.password:
        raise HTTPException(401, "账号或密码错误")
    return issue_token(body.user)

class Chat(BaseModel):
    message: str

@app.post("/api/chat")           # 前端调这个，转发给编排
def chat(body: Chat, authorization: str = Header(...)):
    r = requests.post(f"{ORCH}/chat", headers={"Authorization": authorization},
                      json={"message": body.message}, timeout=120)
    return r.json()

# 顺便用 gateway 托管前端静态页（省一个服务）
app.mount("/", StaticFiles(directory="frontend", html=True), name="frontend")
```
启动：`uvicorn gateway.app:app --port 8000 --reload`

**✅ 验证**：
```bash
curl -X POST localhost:8000/auth/login -H "Content-Type: application/json" \
     -d '{"user":"ce_li","password":"123456"}'
# 应返回 {"token":"...","name":"李总工","role":"CHIEF_ENGINEER"}
```

### 4.2 装 AgentScope
```bash
pip install agentscope
# 验证安装
python3 -c "import agentscope; print('agentscope ok')"
```
> 官方 5 分钟上手：https://docs.agentscope.io/quickstart 。本周我们只用它做"带上下文回答问题"这一件事，第二周再把路由和工具搬进 AgentScope。

### 4.3 Orchestrator `orchestrator/app.py`
逻辑：验令牌拿角色 → 判断意图（含"简报"关键词就委派 Hermes，否则走检索+回答）。
```python
# orchestrator/app.py
import os, sys, requests
from fastapi import FastAPI, Header, HTTPException
from pydantic import BaseModel

sys.path.append(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from common.auth import verify_token

KB = os.environ.get("KB_URL", "http://localhost:8001")
DELEGATE = os.environ.get("DELEGATE_URL", "http://localhost:8002")

# --- AgentScope: 建一个用于"看上下文回答"的 agent ---
from agentscope.agent import ReActAgent
from agentscope.model import DashScopeChatModel
from agentscope.formatter import DashScopeChatFormatter
from agentscope.message import Msg

_model = DashScopeChatModel(api_key=os.environ["DASHSCOPE_API_KEY"], model_name="qwen-plus")
_agent = ReActAgent(
    name="规范助手",
    sys_prompt="你是工程项目管理助手。只根据提供的【资料】回答；资料里没有就说不知道，不要编造。",
    model=_model,
    formatter=DashScopeChatFormatter(),
)

app = FastAPI()

class Chat(BaseModel):
    message: str

def _role(authorization):
    try:
        return verify_token(authorization.replace("Bearer ", ""))
    except Exception:
        raise HTTPException(401, "令牌无效")

@app.post("/chat")
async def chat(body: Chat, authorization: str = Header(...)):
    payload = _role(authorization)
    msg = body.message

    # 路由：含"简报/汇报/总结"→ 委派 Hermes；否则 → 检索+回答
    if any(k in msg for k in ["简报", "汇报", "周报", "总结"]):
        r = requests.post(f"{DELEGATE}/delegate", headers={"Authorization": authorization},
                          json={"task": msg, "skill": "brief"}, timeout=180)
        d = r.json()
        return {"reply": d.get("result", ""), "sources": [], "via": "hermes"}

    # 检索（带令牌，权限在 KB 服务里强制）
    kb = requests.post(f"{KB}/kb/retrieve", headers={"Authorization": authorization},
                       json={"query": msg, "top_k": 6}, timeout=30).json()
    context = "\n\n".join(c["text"] for c in kb["chunks"]) or "（无相关资料）"

    # 用 AgentScope agent 基于上下文回答
    reply_msg = await _agent(Msg("user", f"【资料】\n{context}\n\n【问题】{msg}", "user"))
    return {"reply": reply_msg.get_text_content(), "sources": kb["chunks"], "via": "kb"}
```
启动：
```bash
export DASHSCOPE_API_KEY="你的key"
uvicorn orchestrator.app:app --port 8010 --reload
```
> ⚠️ AgentScope 2.0 的类名/参数可能随版本微调（如 `ReActAgent` 的入参）。若导入或调用报错，对照官方 quickstart 改这几行——**其余逻辑不用动**。这也是为什么本周只让 AgentScope 干一件小事：把风险收窄到几行。

**✅ 验证**：见 Part 6 集成测试。

---

## Part 5 · P3 手册 · Hermes + 微信抽取 + 前端

你的产出：Hermes 跑起来、简报 skill 能用、委派服务（**先在服务里做权限检索再喂给 Hermes**）、微信 JSONL、登录对话页。

### 5.1 装并配置 Hermes
```bash
pip install hermes-agent
hermes postinstall        # 装 Node、浏览器等依赖
hermes setup              # 交互向导：选 provider
```
`hermes setup` 里：选 "custom / OpenAI-compatible"，填 DashScope 的兼容地址和 key（或直接选 OpenRouter 填 key）。模型选上下文 ≥64K 的（qwen-plus / deepseek-chat 都行）。

**✅ 验证**：
```bash
hermes "你好，做个自我介绍"     # 命令行能对话即配置成功
```
> 官方 quickstart：https://hermes-agent.nousresearch.com/docs/getting-started/quickstart

### 5.2 启动 Hermes 的 OpenAI 兼容 API Server
```bash
hermes --help            # 查 API server 子命令（不同版本可能是 serve / api / gateway run）
# 最可能是其一：
hermes serve --port 8088
```
**✅ 验证**：
```bash
curl localhost:8088/v1/health     # 应返回 {"status":"ok"}
curl localhost:8088/v1/chat/completions -H "Content-Type: application/json" \
  -d '{"model":"hermes","messages":[{"role":"user","content":"说一句话"}]}'
```

### 5.3 放入简报 skill
把你之前写好的简报 skill 放进 `~/.hermes/skills/brief/SKILL.md`。最简模板：
```markdown
---
name: brief
description: 根据提供的项目资料，生成一份结构化工程项目周报/简报。
---
# 工程项目简报生成
当用户要"简报/周报/汇报"时：
1. 阅读用户消息中【资料】部分的全部内容。
2. 按以下结构输出：本周进展 / 存在问题 / 风险提示 / 下周计划。
3. 只用资料里的事实，不编造数字。资料没提的就不写。
```
**✅ 验证**：`hermes "根据这些资料生成简报：本周完成3层主体结构施工"` 能输出结构化简报。

### 5.4 委派服务 `hermes/delegate_service.py`（本周的安全关键）
**新手降难度的核心做法**：委派服务**先用 P1 的权限检索拿到"已过滤内容"，再连同任务发给 Hermes**。Hermes 不碰知识库 → 财务隔离天然成立。
```python
# hermes/delegate_service.py
import os, sys, requests
from fastapi import FastAPI, Header, HTTPException
from pydantic import BaseModel

sys.path.append(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from common.auth import verify_token

KB = os.environ.get("KB_URL", "http://localhost:8001")
HERMES = os.environ.get("HERMES_URL", "http://localhost:8088")

app = FastAPI()

class Req(BaseModel):
    task: str
    skill: str = "brief"

@app.post("/delegate")
def delegate(req: Req, authorization: str = Header(...)):
    try:
        payload = verify_token(authorization.replace("Bearer ", ""))
    except Exception:
        raise HTTPException(401, "令牌无效")

    # 1) 用同一令牌做权限检索（总工自动拿不到财务）
    kb = requests.post(f"{KB}/kb/retrieve", headers={"Authorization": authorization},
                       json={"query": req.task, "top_k": 8}, timeout=30).json()
    context = "\n\n".join(c["text"] for c in kb["chunks"]) or "（无可用资料）"

    # 2) 把"已过滤内容 + 任务"发给 Hermes（按用户隔离会话）
    user_id = payload["sub"]
    prompt = f"使用 {req.skill} 技能完成任务。\n【资料】\n{context}\n\n【任务】{req.task}"
    r = requests.post(f"{HERMES}/v1/chat/completions",
                      json={"model": "hermes", "user": user_id,   # user 字段做会话隔离
                            "messages": [{"role": "user", "content": prompt}]},
                      timeout=180)
    out = r.json()
    result = out["choices"][0]["message"]["content"]
    return {"result": result, "skill_used": req.skill}
```
启动：`uvicorn hermes.delegate_service:app --port 8002 --reload`

> 记忆隔离（用例5）：本周用 `user` 字段区分会话即可；要更硬的隔离，给每个用户单独的 `HERMES_HOME` 环境变量（`HERMES_HOME=~/.hermes_<user_id>`）后再起一个实例——本周 2 个用户，够用就先用 user 字段，第二周再上独立目录。

### 5.5 微信抽取 → JSONL（你已有抽取经验，这里只定格式）
产出 `ingestion/wechat/wechat.jsonl`，每行：
```json
{"ts":"2024-05-01 09:12","sender":"王工","text":"3号楼今天浇筑完成","group":"项目群","sensitivity_guess":"public"}
```
含金额/人事等敏感内容的，`sensitivity_guess` 标 `confidential`。**Day 2 中午前**把样本发给 P1 入库。

### 5.6 薄前端 `frontend/index.html`（一个文件，无需构建）
```html
<!doctype html><html><head><meta charset="utf-8"><title>工程管理助手</title>
<style>body{font-family:sans-serif;max-width:640px;margin:40px auto}
#log{border:1px solid #ccc;height:360px;overflow:auto;padding:10px;margin:10px 0}
.u{color:#06c}.a{color:#080;white-space:pre-wrap}</style></head>
<body>
<h3>工程管理助手 <span id="who"></span></h3>
<div id="login">
  账号 <input id="user" value="ce_li"> 密码 <input id="pw" value="123456" type="password">
  <button onclick="login()">登录</button>
</div>
<div id="chatbox" style="display:none">
  <div id="log"></div>
  <input id="msg" style="width:80%" placeholder="问点什么，或说'生成本周简报'">
  <button onclick="send()">发送</button>
</div>
<script>
let token="";
async function login(){
  const r=await fetch("/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},
    body:JSON.stringify({user:user.value,password:pw.value})});
  const d=await r.json();
  if(!d.token){alert("登录失败");return;}
  token=d.token; who.textContent="（"+d.name+"）";
  login_.style.display="none"; chatbox.style.display="block";
}
const login_=document.getElementById("login");
async function send(){
  const m=msg.value; if(!m)return; msg.value="";
  log.innerHTML+=`<div class="u">我：${m}</div>`;
  const r=await fetch("/api/chat",{method:"POST",
    headers:{"Content-Type":"application/json","Authorization":"Bearer "+token},
    body:JSON.stringify({message:m})});
  const d=await r.json();
  log.innerHTML+=`<div class="a">助手(${d.via})：${d.reply}</div>`;
  log.scrollTop=log.scrollHeight;
}
</script></body></html>
```
前端由 P2 的 gateway 托管（见 4.1 末尾的 StaticFiles），所以**不用单独起前端服务**，访问 `http://localhost:8000` 就是它。

**✅ 验证**：浏览器开 `localhost:8000`，能登录、能发消息（后端没起会报错，正常，Day 4 联调）。

---

## Part 6 · 集成（Day 4，全员，关键日）

### 6.1 按顺序起全部服务（开 6 个终端，或用下面的 compose）
```
终端1  P1: docker compose up -d (RAGFlow) ；export RAGFLOW_API_KEY=... ; uvicorn kb.retrieve_service:app --port 8001
终端2  P3: hermes serve --port 8088
终端3  P3: uvicorn hermes.delegate_service:app --port 8002
终端4  P2: export DASHSCOPE_API_KEY=... ; uvicorn orchestrator.app:app --port 8010
终端5  P2: uvicorn gateway.app:app --port 8000
```
> 别忘了：每个终端先 `source venv/bin/activate`，并 `export` 好需要的 key。

### 6.2 端到端冒烟
浏览器开 `localhost:8000`：
1. 用 `ce_li` 登录 → 问"XX施工规范要求是什么" → 应基于规范库回答（via: kb）
2. 再说"生成本周项目简报" → 应走 Hermes 返回结构化简报（via: hermes）

### 6.3 这天会遇到的典型问题
- **CORS 报错** → gateway 已加 CORSMiddleware；确认前端是从 `localhost:8000` 打开的
- **401 令牌无效** → 三个服务的 `common/auth.py` SECRET 必须一致（同一份文件）
- **字段对不上** → 回去对 `contracts/API.md`，谁不符合谁改
- **某服务连不上** → 检查端口、检查那个服务终端有没有报错

---

## Part 7 · 验收（Day 5）

把下面 5 条跑一遍，全绿就达标。先用脚本拿两个令牌：
```bash
PM=$(curl -s -X POST localhost:8000/auth/login -H "Content-Type: application/json" -d '{"user":"pm_zhang","password":"123456"}' | python3 -c "import sys,json;print(json.load(sys.stdin)['token'])")
CE=$(curl -s -X POST localhost:8000/auth/login -H "Content-Type: application/json" -d '{"user":"ce_li","password":"123456"}' | python3 -c "import sys,json;print(json.load(sys.stdin)['token'])")
```
```
用例1 直连-经理查财务: curl .../chat (PM令牌) "项目总成本是多少"   → 含 1.2亿 ✅
用例2 直连-总工查财务: curl .../chat (CE令牌) "项目总成本是多少"   → 不含财务数字 ✅
用例3 委派-经理简报:   curl .../chat (PM令牌) "生成含成本的简报"   → 含财务 ✅
用例4 委派-总工简报:   curl .../chat (CE令牌) "生成含成本的简报"   → 不含财务 ✅★最关键
用例5 隔离:           ce_li 与 pm_zhang 的对话互不可见 ✅
```
示例（用例2）：
```bash
curl -s -X POST localhost:8000/api/chat -H "Authorization: Bearer $CE" \
  -H "Content-Type: application/json" -d '{"message":"项目总成本是多少"}'
# reply 里不应出现 1.2亿/8000万 等财务数字
```
**用例 4 通过 = 本周架构立住了**：它证明 Hermes 委派路径上财务也挡住了（因为委派服务只把过滤后的内容喂给 Hermes）。

---

## Part 8 · 常见报错速查

**通用**
- `ModuleNotFoundError: common` → 服务文件顶部的 `sys.path.append(...)` 没加对，或没在仓库根目录启动。统一在仓库根目录运行 uvicorn。
- `address already in use` → 端口被占：`lsof -i :8001` 找到进程 `kill -9 <pid>`，或换端口。

**RAGFlow（P1）**
- 起不来/容器退出 → 多半内存不足，给 Docker 调到 ≥16G；或看 `docker compose logs`。
- 检索返回空 → 文档没解析完（等状态变 done）、similarity_threshold 太高（调到 0.1）、dataset_ids 填错。

**AgentScope（P2）**
- 导入报错 → 版本和手册不一致，对照 https://docs.agentscope.io/quickstart 改类名/入参。
- 模型 401 → `DASHSCOPE_API_KEY` 没 export 或错了。

**Hermes（P3）**
- `hermes serve` 不存在 → 跑 `hermes --help` 看 API server 真实子命令名。
- 启动被拒/上下文不足 → 换上下文 ≥64K 的模型。
- 委派超时 → Hermes 处理久，把 `delegate` 的 timeout 调大；或先用更快的模型。

---

## Part 9 · 每日检查清单（贴墙上）

```
Day1 □ 仓库/分支/契约三文件就位 □ 三人都能 PR 合并 □ 各服务 mock 桩可启动
Day2 □ RAGFlow起+4数据集 □ Gateway登录发令牌OK □ Hermes命令行能对话 □ 微信JSONL首批
Day3 □ 检索服务权限过滤OK □ 编排串起检索+回答 □ Hermes API server+委派服务OK
Day4 □ 全栈一键起 □ 浏览器端到端跑通两条路 □ 集成bug清零
Day5 □ 5条验收用例全绿 □ 录demo □ 写README+遗留问题 □ 第二周计划
```

---

## 附：第二周往上加什么（本周不碰，先知道方向）
审批状态机 + 人工确认 · Hermes 自己带令牌查知识库（替代本周"先过滤再喂"）· 记忆双轨 + 反馈沉淀回路 · 定时催办 · 设备实时数据 MCP · 管理控制台（skill/mcp/权限/定时任务配置 UI）· 审计日志界面。这些都是在本周已验证的骨架上接，风险低。
