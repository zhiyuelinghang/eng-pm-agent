# Dobby 工程管理平台服务器更新说明

本文适用于当前 Windows 服务器与便携 Python 部署方式。更新包采用“程序代码”
与“运行数据”分离的结构，解压更新不会包含 `.env`、平台数据库或 AgentScope
运行数据。

## 一、更新包包含什么

`生成服务器完整更新包.bat` 会在开发机完成两套前端构建，并生成：

```text
更新文件\dobby-server-update.zip
更新文件\dobby-server-first-install.zip
```

`dobby-server-first-install.zip` 用于服务器第一次增加 AgentScope，包含：

- 平台 `backend` 与 `frontend`；
- AgentScope 核心与 Dobby 智能体管理端；
- AgentScope/Dobby 接入脚本；
- 首次初始化、统一启动、统一停止脚本；
- 两套已经在开发机完成构建的前端页面；
- 完整的 `python-3.13.14` 便携运行时与全部 Python 依赖。

`dobby-server-update.zip` 用于完成首次部署后的日常代码更新，不重复携带
体积较大的便携 Python 运行时。

压缩包明确不包含：

- 根目录 `.env`；
- `data\engpm.db` 平台数据库；
- `data\agentscope` 中的模型凭证、智能体、知识库与会话数据；
- `node_modules` 和构建缓存。

服务器不需要安装 Node.js、npm 或 pnpm，也不需要联网执行 pip。Node.js
只在生成更新包的开发机上用于构建前端。

## 二、首次部署 AgentScope

1. 备份服务器根目录 `.env` 和 `data` 目录。
2. 关闭旧的平台启动窗口；如果已运行 AgentScope，执行
   `一键停止全部服务.bat`。
3. 第一次部署使用 `dobby-server-first-install.zip`，将压缩包内
   `server-first-install` 目录中的内容覆盖到服务器项目根目录。
4. 不要删除服务器原有 `.env` 和 `data`。
5. 在服务器 `.env` 中补充 AgentScope 配置：

```env
AGENTSCOPE_BASE_URL=http://127.0.0.1:18642
AGENTSCOPE_ADMIN_USERNAME=服务器管理账号
AGENTSCOPE_ADMIN_PASSWORD=高强度管理密码
AGENTSCOPE_AUTH_SECRET=至少32位随机字符串
AGENTSCOPE_SERVICE_TOKEN=至少32位随机字符串
DOBBY_AGENT_TOOL_TOKEN=
AGENTSCOPE_GLOBAL_CONFIG_ID=default
AGENTSCOPE_MANAGEMENT_TOKEN_TTL_SECONDS=28800
AGENTSCOPE_REQUEST_TIMEOUT_SECONDS=150
AGENTSCOPE_POLL_INTERVAL_SECONDS=0.35
```

6. 运行 `服务器首次部署检查.bat`。该脚本只验证随包携带的代码、预构建页面、
   便携 Python 及 AgentScope 依赖，不安装任何软件。
7. 检查成功后运行 `服务器一键启动.bat`。

## 三、迁移当前 AgentScope 配置

模型凭证、Dobby 全局总控、知识库与向量数据不属于代码。如果需要把开发机上已经
配置好的内容一起迁移：

1. 先停止开发机和服务器上的 AgentScope。
2. 单独压缩开发机的 `data\agentscope`。
3. 通过安全通道复制到服务器同名目录。
4. 只迁移 `data\agentscope`，不要用开发机的整个 `data` 覆盖服务器。
5. 该目录包含模型凭证等敏感数据，不得提交 Git 或放入普通代码更新包。

如果不迁移，服务器 AgentScope 会以空配置启动，需要在 Dobby 管理端重新配置
凭证、模型、知识库与全局主智能体。

## 四、以后更新

后续版本只需：

1. 在开发机运行 `生成服务器完整更新包.bat`，脚本会先构建两套前端。
2. 备份服务器 `.env` 和 `data`。
3. 执行服务器 `一键停止全部服务.bat`。
4. 解压 `dobby-server-update.zip` 并覆盖项目代码。
5. 如果版本说明明确要求更新便携 Python，再改用首次安装包覆盖。
6. 执行服务器上的 `服务器一键启动.bat`。

服务器通过便携 Python 提供预构建静态页面和内部 API 反向代理，不运行 Vite
开发服务器。正式公网生产环境仍建议进一步改为 Windows 服务和统一反向代理。
