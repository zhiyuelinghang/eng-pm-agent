# Dobby Windows 服务器部署

本文适用于项目内嵌 Python 与预构建前端的部署方式。开发启动见 [项目入口](README.md)，
发布包的实际内容以 [生成服务器完整更新包.bat](生成服务器完整更新包.bat) 为准。

## 更新包与数据库 SQL

在开发机运行 `生成服务器完整更新包.bat`，脚本构建 Vue 业务端和 React 管理端，并生成：

| 文件 | 用途 |
| --- | --- |
| `更新文件\dobby-server-first-install.zip` | 首次部署，包含代码、两套前端产物、完整便携 Python 与 Centrifugo 运行程序 |
| `更新文件\dobby-server-update.zip` | 日常代码更新，保留服务器现有 Python 主体与 Centrifugo 程序 |

两个包均包含后端、前端、AgentScope、运行脚本、`utils` 和任务引擎源码。它们不包含
根目录 `.env`、`data`、PostgreSQL 数据备份和 Centrifugo 本机生成的配置。其他已安装
MCP、技能、知识文件和工作区属于运行资产，不能仅靠代码包迁移。

**当前打包脚本不复制 `数据库更新/`。** 有数据库变更的版本必须单独交付已审核 SQL，
明确目标数据库、前置版本与执行顺序，并对照
[数据库更新目录](数据库更新) 和 [Alembic 迁移](backend/alembic/versions) 核对。
目录中的增量 SQL 不等于完整的新库初始化方案；全新数据库也需事先准备本版本要求的
Schema、扩展和表结构。SQL 未交付或数据库版本未核对完成时，不应启动新代码。

平台本体的生产运行不需要 Node.js、npm、pnpm 或联网执行 pip。前端在开发机完成构建；
另行安装的 MCP 仍须携带其约定的运行时与依赖，详见
[MCP 包上传与运行说明](docs/MCP包上传与运行说明.md)。

## 数据备份与迁移

当前标准配置将平台业务、AgentScope 管理与会话、记忆和向量数据保存在 PostgreSQL，
本地目录保存文件和工具资产。备份或迁移时分别核对：

| 数据 | 当前来源及处理 |
| --- | --- |
| 平台业务与任务 | `DATABASE_URL` 指向的数据库，通常使用 `platform`、`task_engine` Schema |
| 模型凭证、智能体、会话及管理配置 | PostgreSQL `agentscope` Schema；可由 `AGENTSCOPE_DATABASE_URL` 指向独立数据库 |
| 知识目录与向量 | PostgreSQL `knowledge` Schema，包含按知识库建立的动态向量表；可由 `AGENTSCOPE_KNOWLEDGE_DATABASE_URL` 独立配置 |
| 记忆与学习数据 | PostgreSQL `memory` Schema；核对 `MEMORY_DATABASE_URL` 与实际 Schema 设置 |
| 本地文件与工具 | `data/uploads`、`data/agentscope` 及自定义存储路径；包括工作区、知识原文件、MCP 注册目录和技能注册目录 |
| 配置与密钥 | 根目录 `.env` 及部署使用的外部配置；单独保管，不放进普通更新包或 Git |

准确的连接优先级与可配置目录见
[scripts/agentscope_dev_app.py](scripts/agentscope_dev_app.py)、
[backend/app/config.py](backend/app/config.py) 和 [utils/config.py](utils/config.py)。
使用自定义目录或独立数据库时，按实际配置扩展备份范围。WeKnora 等外部服务的数据
另按其部署方式备份，不能认为已包含在本项目 `data` 中。

迁移操作顺序：

1. 确认目标已有业务数据的保留范围，停止源端和目标端相关写入服务，保存目标备份。
2. 备份并恢复所需 PostgreSQL 数据、Schema、扩展及动态向量表；保持关联数据与版本一致。
3. 复制同一备份时点的本地运行文件，核对路径与数据库引用，不用开发机整份 `data`
   无差别覆盖正在使用的服务器。
4. 配置目标连接地址和管理认证，保留已加密配置依赖的密钥。平台连接密文使用
   `CONNECTOR_SECRET_KEY`，未配置时从 `JWT_SECRET` 派生；直接更换会影响历史密文解密。
5. 完成数据库版本、文件和配置检查后启动，验证模型配置、已有会话、知识检索和附件访问。

只复制 `data/agentscope` 不会迁移 PostgreSQL 中的凭证、智能体或会话。若不迁移旧数据，
新环境仍需先准备数据库结构，再在管理端配置模型、工具与智能体。

## 首次部署

1. 准备 PostgreSQL 和本版本所需结构；参考 `.env.example` 配置服务器 `.env`，至少核对
   数据库、`JWT_SECRET`、AgentScope 管理账号、签名密钥和服务令牌。
2. 如服务器已有服务，执行 `一键停止全部服务.bat`，按上一节备份数据库、文件和密钥。
   停止脚本只结束登记且身份匹配的项目服务，其他端口占用需另行处理。
3. 将首次部署包内 `server-first-install` 的内容复制到项目根目录，保留服务器运行数据。
4. 单独接收并审核该版本需要的 SQL，在正确数据库按顺序执行；执行失败时先恢复或修正，
   不继续启动新版本。
5. 运行 `服务器首次部署检查.bat`。它检查便携运行环境、配置和预构建页面，不安装依赖，
   不执行数据库迁移，也不能替代数据库版本与业务验证。缺少 `.env` 时会从示例生成并
   要求完成配置后重新检查。
6. 运行 `服务器一键启动.bat`，检查平台、智能体服务和群聊连接，并核对初始管理员账号。
   平台管理员由 [main.py](backend/app/main.py) 的 `seed_admin` 初始化，当前不是通过
   管理员环境变量配置密码；首次使用后应修改初始密码。

首次包已包含 Centrifugo，`安装群聊实时服务.bat` 只用于损坏修复或管理员升级，执行时
会下载并校验程序。日常启动使用 `start-centrifugo.bat`。

## 日常更新

1. 开发机完成发布验证，再运行 `生成服务器完整更新包.bat` 构建代码包。
2. 核对是否存在数据库变更，另行准备 SQL、适用版本和执行顺序。
3. 停止服务器服务，备份 PostgreSQL、本地运行文件与 `.env`。
4. 将日常包内 `server-update` 的内容复制到项目根目录；需要更新 Python 依赖时使用
   已验证的完整运行时或首次部署包，不假定日常包会补齐依赖。
5. 执行已审核且尚未应用的版本 SQL，核对成功后再运行 `服务器一键启动.bat`。
6. 检查登录、项目数据、智能体会话、知识检索与任务/群聊关键链路；异常时依据该版本
   恢复方案处理，不反复覆盖文件代替定位。

平台和 AgentScope 标准启动不会执行 Alembic 或自动迁移元数据表。管理员显式执行的
初始化、迁移脚本仍会改变数据库；不要把它们当作只读检查。知识库等正常业务运行也
可能创建动态存储对象，备份需覆盖这些对象。

## 对外访问

服务器通过便携 Python 提供前端静态页面和内部 API 代理，不运行 Vite 开发服务器。
生产通过 Nginx 提供 HTTPS，并转发 API、`/agentscope-api` 与 `/realtime/` WebSocket。
按实际公网域名设置 `CORS_ORIGINS`、`FRONTEND_PUBLIC_URL` 和 `CENTRIFUGO_WS_URL`。

Centrifugo 使用 `CENTRIFUGO_BIND_ADDRESS=127.0.0.1`、`CENTRIFUGO_PORT=38431`，
Nginx 将 `/realtime/` 转发到本机该端口，无需向公网开放 38431。
