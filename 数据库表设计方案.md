# Dobby 工程管理平台数据表设计

## 设计边界

```text
业务字段只取自用户提供的工程描述、WBS、风险清单、人员名单及后续明确提出的登录要求。
仅额外增加主键、外键、父子关系、排序号、创建时间和更新时间等必要数据库字段。
```

## 第一部分：项目基本信息

### 表关系

```text
projects
```

## 1. `projects`

| 字段 | 类型 | 可空 | 来源 | 说明 |
|---|---|---:|---|---|
| `id` | INTEGER | 否 | 系统必要 | 主键 |
| `name` | VARCHAR(200) | 否 | 项目名称 | 项目名称 |
| `engineering_type_description` | TEXT | 是 | 工程类型 | 工程用途及功能说明；创建项目后补充 |
| `contract_start_date` | DATE | 是 | 项目阶段 | 合同工期开始日期 |
| `contract_end_date` | DATE | 是 | 项目阶段 | 合同工期结束日期 |
| `contract_duration_days` | INTEGER | 是 | 项目阶段 | 合同总工期，日历天 |
| `contract_amount_wan_yuan` | NUMERIC(18,2) | 是 | 项目阶段 | 合同额，万元 |
| `construction_unit_name` | VARCHAR(300) | 是 | 建设单位 | 建设单位名称；创建项目后补充 |
| `general_contractor_unit_name` | VARCHAR(300) | 是 | 施工总承包单位 | 施工总承包单位名称；创建项目后补充 |
| `supervision_unit_name` | VARCHAR(300) | 是 | 监理单位 | 监理单位名称；创建项目后补充 |
| `design_unit_name` | VARCHAR(300) | 是 | 设计单位 | 设计单位名称；创建项目后补充 |
| `survey_unit_name` | VARCHAR(300) | 是 | 勘察单位 | 勘察单位名称；创建项目后补充 |
| `created_at` | DATETIME | 否 | 系统必要 | 创建时间 |
| `updated_at` | DATETIME | 否 | 系统必要 | 更新时间 |

约束与索引：

```text
PRIMARY KEY (id)
CHECK (contract_duration_days IS NULL OR contract_duration_days > 0)
CHECK (contract_amount_wan_yuan IS NULL OR contract_amount_wan_yuan >= 0)
CHECK (
    contract_end_date IS NULL
    OR contract_start_date IS NULL
    OR contract_end_date >= contract_start_date
)
INDEX (name)
```

## 第二部分：WBS 与总进度计划

### 表关系

```text
projects
 └── 1 : N ── project_wbs_items
                 ├── 1 : N ── project_wbs_items（父子树）
                 ├── N : N ── project_wbs_predecessors
                 └── 1 : 0..1 ── project_wbs_quality_requirements
```

## 1. `project_wbs_items`

| 字段 | 类型 | 可空 | 来源 | 说明 |
|---|---|---:|---|---|
| `id` | INTEGER | 否 | 系统必要 | 主键 |
| `project_id` | INTEGER | 否 | 项目 | 外键 `projects.id` |
| `parent_id` | INTEGER | 是 | WBS 编号及级别 | 父节点；用于保存树结构 |
| `sort_order` | INTEGER | 否 | Excel 行顺序 | 同级显示顺序 |
| `color_value` | VARCHAR(50) | 是 | 颜色 | 原始颜色字段值 |
| `wbs_code` | VARCHAR(128) | 否 | WBS 编号 | WBS 编号 |
| `name` | VARCHAR(300) | 否 | 层级名称列、任务名称 / 标题 | 节点名称 |
| `assigned_to_text` | VARCHAR(300) | 是 | 分配给 | 原始分配对象 |
| `planned_start_at` | DATETIME | 是 | 计划开始日期 | 计划开始时间 |
| `planned_finish_at` | DATETIME | 是 | 计划结束日期 | 计划结束时间 |
| `deadline_at` | DATETIME | 是 | 截止日期 | 截止时间 |
| `progress_percent` | NUMERIC(5,2) | 是 | 进度 (%) | 完成百分比 |
| `duration_hours` | NUMERIC(12,2) | 是 | 持续时间 (小时) | 持续时间，小时 |
| `estimated_hours` | NUMERIC(12,2) | 是 | 预计小时数 | 预计投入小时数 |
| `time_log_minutes` | INTEGER | 是 | 时间日志 (分钟) | 已记录分钟数 |
| `status_text` | VARCHAR(100) | 是 | 状态 | 原始状态 |
| `priority_text` | VARCHAR(100) | 是 | 优先级 | 原始优先级 |
| `description` | TEXT | 是 | 任务描述 | 任务描述 |
| `budget` | NUMERIC(18,2) | 是 | 预算 | 原始预算 |
| `actual_cost` | NUMERIC(18,2) | 是 | 实际成本 | 原始实际成本 |
| `msp_uid` | VARCHAR(100) | 是 | MSP UID | Microsoft Project UID |
| `msp_id` | VARCHAR(100) | 是 | MSP ID | Microsoft Project ID |
| `source_created_at` | DATETIME | 是 | 创建于 | 来源记录创建时间 |
| `source_creator` | VARCHAR(200) | 是 | 创建者 | 来源记录创建者 |
| `item_type` | VARCHAR(100) | 是 | 类型 | 项目、任务组或任务 |
| `source_project_path` | TEXT | 是 | 项目 | 来源文件中的项目路径 |
| `level` | SMALLINT | 否 | 级别 | WBS 层级 |
| `created_at` | DATETIME | 否 | 系统必要 | 数据库创建时间 |
| `updated_at` | DATETIME | 否 | 系统必要 | 数据库更新时间 |

约束与索引：

```text
PRIMARY KEY (id)
FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
FOREIGN KEY (parent_id) REFERENCES project_wbs_items(id)
UNIQUE (project_id, wbs_code)
CHECK (parent_id IS NULL OR parent_id <> id)
CHECK (sort_order >= 0)
CHECK (level > 0)
CHECK (
    planned_finish_at IS NULL
    OR planned_start_at IS NULL
    OR planned_finish_at >= planned_start_at
)
CHECK (progress_percent IS NULL OR progress_percent BETWEEN 0 AND 100)
CHECK (duration_hours IS NULL OR duration_hours >= 0)
CHECK (estimated_hours IS NULL OR estimated_hours >= 0)
CHECK (time_log_minutes IS NULL OR time_log_minutes >= 0)
CHECK (budget IS NULL OR budget >= 0)
CHECK (actual_cost IS NULL OR actual_cost >= 0)
INDEX (project_id, parent_id, sort_order)
INDEX (project_id, level)
INDEX (msp_uid)
INDEX (msp_id)
```

业务约束：

```text
父节点与子节点必须属于同一项目
树中禁止出现循环引用
```

## 2. `project_wbs_predecessors`

| 字段 | 类型 | 可空 | 来源 | 说明 |
|---|---|---:|---|---|
| `id` | INTEGER | 否 | 系统必要 | 主键 |
| `wbs_item_id` | INTEGER | 否 | 前任 | 当前 WBS 节点 |
| `predecessor_wbs_item_id` | INTEGER | 否 | 前任 | 前任 WBS 节点 |
| `created_at` | DATETIME | 否 | 系统必要 | 创建时间 |

约束与索引：

```text
PRIMARY KEY (id)
FOREIGN KEY (wbs_item_id)
    REFERENCES project_wbs_items(id) ON DELETE CASCADE
FOREIGN KEY (predecessor_wbs_item_id)
    REFERENCES project_wbs_items(id)
UNIQUE (wbs_item_id, predecessor_wbs_item_id)
CHECK (wbs_item_id <> predecessor_wbs_item_id)
INDEX (wbs_item_id)
INDEX (predecessor_wbs_item_id)
```

业务约束：

```text
当前节点与前任节点必须属于同一项目
```

## 3. `project_wbs_quality_requirements`

| 字段 | 类型 | 可空 | 来源 | 说明 |
|---|---|---:|---|---|
| `id` | INTEGER | 否 | 系统必要 | 主键 |
| `project_id` | INTEGER | 否 | 系统必要 | WBS 节点所属项目 |
| `wbs_code` | VARCHAR(128) | 否 | WBS编号 | 对应的 WBS 编号 |
| `quality_acceptance_item` | TEXT | 否 | 质量验收项 | 质量验收内容 |
| `control_indicator` | TEXT | 否 | 控制指标 | 质量控制指标 |
| `inspection_frequency` | TEXT | 否 | 检查频次 | 检查频次要求 |
| `related_documents` | TEXT | 否 | 关联资料 | 需要关联的资料原文 |
| `created_at` | DATETIME | 否 | 系统必要 | 数据库创建时间 |
| `updated_at` | DATETIME | 否 | 系统必要 | 数据库更新时间 |

约束与索引：

```text
PRIMARY KEY (id)
FOREIGN KEY (project_id, wbs_code)
    REFERENCES project_wbs_items(project_id, wbs_code)
    ON UPDATE CASCADE
    ON DELETE CASCADE
UNIQUE (project_id, wbs_code)
```

业务约束：

```text
通过项目与 WBS 编号共同确定对应的 WBS 节点
质量指标可以关联任意层级的 WBS 节点，不限于叶子节点
原始质量字段全部为空的 WBS 节点不创建质量指标记录
工序名称直接读取 project_wbs_items.name，不在本表重复保存
```

## 第三部分：工程风险清单

### 表关系

```text
projects
 └── 1 : N ── project_risks ── N : 0..1 ── project_wbs_items
```

## 1. `project_risks`

| 字段 | 类型 | 可空 | 来源 | 说明 |
|---|---|---:|---|---|
| `id` | INTEGER | 否 | 系统必要 | 主键 |
| `project_id` | INTEGER | 否 | 项目 | 外键 `projects.id` |
| `serial_no` | INTEGER | 否 | 序号 | 原清单序号 |
| `related_wbs_item_id` | INTEGER | 是 | 关联工序 | 匹配成功时关联 `project_wbs_items.id` |
| `related_process_name` | VARCHAR(300) | 否 | 关联工序 | 原始工序名称 |
| `risk_part` | VARCHAR(300) | 否 | 风险部位 | 风险部位 |
| `risk_level` | VARCHAR(50) | 否 | 风险等级 | 原始风险等级 |
| `evaluation_condition` | TEXT | 否 | 参照的风险评价条件 | 风险评价条件原文 |
| `risk_window_start_date` | DATE | 是 | 风险窗口 | 风险窗口开始日期 |
| `risk_window_end_date` | DATE | 是 | 风险窗口 | 风险窗口结束日期 |
| `summary` | TEXT | 是 | 风险情况简述 | 风险情况简述 |
| `created_at` | DATETIME | 否 | 系统必要 | 数据库创建时间 |
| `updated_at` | DATETIME | 否 | 系统必要 | 数据库更新时间 |

约束与索引：

```text
PRIMARY KEY (id)
FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
FOREIGN KEY (related_wbs_item_id) REFERENCES project_wbs_items(id)
UNIQUE (project_id, serial_no)
CHECK (
    risk_window_end_date IS NULL
    OR risk_window_start_date IS NULL
    OR risk_window_end_date >= risk_window_start_date
)
INDEX (project_id)
INDEX (related_wbs_item_id)
INDEX (risk_level)
INDEX (risk_window_start_date, risk_window_end_date)
```

业务约束：

```text
风险与关联 WBS 节点必须属于同一项目
```

## 第四部分：项目人员名单

### 表关系

```text
projects
 └── 1 : N ── project_personnel_assignments ── N : 1 ── users
```

## 1. `users`

| 字段 | 类型 | 可空 | 来源 | 说明 |
|---|---|---:|---|---|
| `id` | INTEGER | 否 | 系统必要 | 主键 |
| `username` | VARCHAR(64) | 否 | 用户明确要求支持登录 | 登录名 |
| `password_hash` | VARCHAR(255) | 否 | 用户明确要求支持登录 | 登录密码哈希，不保存明文密码 |
| `role` | VARCHAR(20) | 否 | 系统登录与权限控制必要 | 系统角色：`admin` 为管理员，`user` 为普通用户 |
| `real_name` | VARCHAR(100) | 否 | 姓名 | 姓名 |
| `identity_card_no` | VARCHAR(30) | 否 | 身份证 | 身份证号码，按文本保存 |
| `created_at` | DATETIME | 否 | 系统必要 | 数据库创建时间 |
| `updated_at` | DATETIME | 否 | 系统必要 | 数据库更新时间 |

约束与索引：

```text
PRIMARY KEY (id)
UNIQUE (username)
UNIQUE (identity_card_no)
CHECK (role IN ('admin', 'user'))
INDEX (real_name)
INDEX (role)
```

## 2. `project_personnel_assignments`

| 字段 | 类型 | 可空 | 来源 | 说明 |
|---|---|---:|---|---|
| `id` | INTEGER | 否 | 系统必要 | 主键 |
| `project_id` | INTEGER | 否 | 系统必要 | 外键 `projects.id` |
| `user_id` | INTEGER | 否 | 系统必要 | 外键 `users.id` |
| `serial_no` | INTEGER | 否 | 序号 | 名单序号 |
| `position_name` | VARCHAR(100) | 否 | 职务 | 职务名称 |
| `certificate_no` | VARCHAR(100) | 否 | 证书编号 | 证书编号，按文本保存 |
| `responsibility_description` | TEXT | 否 | 职责描述 | 职责描述 |
| `created_at` | DATETIME | 否 | 系统必要 | 数据库创建时间 |
| `updated_at` | DATETIME | 否 | 系统必要 | 数据库更新时间 |

约束与索引：

```text
PRIMARY KEY (id)
FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
FOREIGN KEY (user_id) REFERENCES users(id)
UNIQUE (project_id, serial_no)
INDEX (project_id)
INDEX (user_id)
INDEX (position_name)
INDEX (certificate_no)
```
