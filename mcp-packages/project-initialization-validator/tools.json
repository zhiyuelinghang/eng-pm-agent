[
  {
    "name": "validate_project_initialization",
    "title": "核验项目初始化草稿",
    "description": "对平台已经结构化入库的项目初始化草稿执行确定性规则核验，返回稳定规则编号、草稿数据 ID、字段名、中文标签、详细说明和处理建议。",
    "inputSchema": {
      "type": "object",
      "properties": {
        "draft": {
          "type": "object",
          "description": "平台组装后的标准项目初始化草稿，包含 project、personnel、wbs、risks、quality_requirements 五个分区；每条业务数据带数据库分配的 record_id。"
        }
      },
      "required": ["draft"],
      "additionalProperties": false
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    }
  }
]
