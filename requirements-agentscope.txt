# AgentScope Python 2.0.6 发布版本及其服务、SQL 存储、知识库依赖。
# 运行时向量存储已经统一到 PostgreSQL/pgvector。
agentscope[rag,service,storage-sql]==2.0.6
# 仅供 scripts/migrate_legacy_storage.py 读取旧本地 Qdrant 数据。
qdrant-client>=1.13,<2.0

# AsyncSQLAlchemyStorage 的 SQLite 异步驱动由调用方显式安装。
aiosqlite==0.22.1
# 统一 PostgreSQL 存储使用 SQLAlchemy 的 asyncpg 异步驱动。
asyncpg>=0.30,<1.0
# 统一记忆核心与 memory schema 的 PostgreSQL/pgvector 访问。
mem0ai==2.0.12
psycopg[binary]==3.3.4
psycopg-pool==3.3.1
pgvector>=0.4,<1.0

# 记忆编排、生命周期、时间图谱与 GraphRAG。
langgraph==1.1.6
langgraph-checkpoint==4.1.1
langgraph-checkpoint-postgres==3.1.0
graphiti-core==0.29.2
neo4j==6.2.0
lightrag-hku>=1.0.0
networkx==3.6.1

# 记忆模型调用与文本处理。
openai>=2.30.0,<3.0
httpx==0.28.1
aiohttp>=3.13.5,<4.0
tiktoken==0.12.0
python-dotenv==1.2.2
tenacity==9.1.4
rich>=14.3.3,<16.0
loguru==0.7.3
regex==2026.4.4

# 独立 BGE 嵌入模型。固定 CPU 构建，避免 Windows 工作站误装 CUDA 依赖。
sentence-transformers==5.6.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0+cpu
transformers==5.14.1
tokenizers==0.22.2
safetensors==0.8.0
huggingface-hub==1.9.0
sentencepiece>=0.2.0
numpy>=2.4.4,<3.0
scipy==1.17.1
scikit-learn==1.9.0

# 仅供显式切回 AGENTSCOPE_STORAGE=memory 时使用；默认持久化使用 PostgreSQL
fakeredis==2.37.0

# Dobby 项目初始化附件的结构化读取与本地 OCR。
openpyxl>=3.1,<4.0
xlrd>=2.0,<3.0
python-docx>=1.1,<2.0
python-pptx>=1.0,<2.0
pdfplumber>=0.11,<1.0
pypdfium2>=4.30,<6.0
Pillow>=10.0,<13.0
rapidocr-onnxruntime>=1.2,<2.0
