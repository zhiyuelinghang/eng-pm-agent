# AgentScope Python 发布版本及其服务、Redis 存储、知识库依赖
# rag 额外依赖包含本地 Qdrant、PDF、Word、PowerPoint、Excel 解析支持。
agentscope[rag,service,storage]==2.0.4.post1

# 无 Docker/Redis 服务时使用的本地开发存储；生产环境改用真实 Redis
fakeredis==2.37.0
