# AgentScope Python 发布版本及其服务、存储、知识库依赖
# rag 额外依赖包含本地 Qdrant、PDF、Word、PowerPoint、Excel 解析支持。
agentscope[rag,service,storage]==2.0.4.post1

# 仅供显式切回 AGENTSCOPE_STORAGE=memory 时使用；默认持久化使用 SQLite
fakeredis==2.37.0
