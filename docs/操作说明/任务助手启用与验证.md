# 任务助手启用与验证

2026-09-09 已在本地实际运行环境完成配置。

## 当前配置

- 沿用原“任务智能体”，统一命名为“任务助手”，没有创建重复对象。智能体 ID：`969df9f1063348728ce569f596c30ed7`。
- 已接入平台主智能体的任务助手职责并启用，固定模型为 DeepSeek V4 Flash。
- 沿用已分配的任务引擎 MCP；开放总控调用并补充协作说明。
- 只负责整理任务需求、清理上下文、调用任务引擎生成待用户确认的草稿。发布任务和创建执行计划由平台确认流程处理。
- 保留原模型凭据、记忆权限和学习参与设置；其他三个主智能体的职责对象保持原配置。

## 本次接通的兼容问题

实际 MCP 工具名称为 `mcp__task-engine__generate_task_flow`。平台原来只接受无前缀名称，会把真实成功调用误判为未调用任务引擎。现在兼容两种准确名称，同时保留真实工具调用校验。

任务引擎原生结果使用 `trigger`、`assignee`、`deliverable` 等字段。平台接收时补充转换为草稿编辑器使用的执行时间、人员、工点和交付材料字段；既有平台格式继续兼容。转换只生成草稿数据，不执行发布。

## 已完成的验证

1. 真实管理页面显示“任务助手 · 已配置”，固定模型和 MCP 可用。
2. 真实调试会话调用任务引擎，生成来源为 AI，使用 `save=false`，没有保存可复用任务流。
3. 在工程平台现有 `test` 项目的主页输入 `@任务助手`，生成“施工资料完整性核查”可编辑草稿。2026-09-10 10:30 的执行时间、任务节点和交付材料正确保留，缺失的责任人、确认人和工点留空。
4. 验证后通过页面取消测试草稿；草稿 9 的状态为 `dismissed`，发布消息为空，已发布任务列表为空。
5. 任务助手 MCP 兼容测试和项目群聊相关测试共 31 项通过。平台后端已重新加载修复并通过健康检查。

本次配置经管理 API 保存，没有数据库结构升级，也没有增加启动时自动配置逻辑。
